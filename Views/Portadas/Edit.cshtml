@model Biblio.Models.Portada

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Portada</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <label asp-for="IdLibr" class="control-label"></label>
                <input asp-for="IdLibr" class="form-control" />
                <span asp-validation-for="IdLibr" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="portada" class="control-label"></label>
                <input asp-for="portada" class="form-control" />
                <span asp-validation-for="portada" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
